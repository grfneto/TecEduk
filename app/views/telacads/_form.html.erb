<%= form_for(@telacad) do |f| %>
  <% if @telacad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@telacad.errors.count, "error") %> prohibited this telacad from being saved:</h2>

      <ul>
      <% @telacad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
